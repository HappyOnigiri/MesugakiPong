# Architecture Map (MesugakiPong)

このドキュメントは、コードやファイル構成から即座に読み取れない「設計意図」や「勘違いしやすいポイント」を補足するための一次情報です。

## 1. 全体アーキテクチャの特記事項

- **単一アプリ構成**: 本リポジトリは「MesugakiPong」のみの単体リポジトリです（過去に複数ゲームのポータルとして運用されていた名残がコミット履歴などに含まれていますが、現在は MesugakiPong 専用に再構築されています）。
- **アセットの参照**: 以前の構成で使用されていた `@shared` のようなパスエイリアスは廃止されています。リソースは通常の相対パスで `src/assets/` 等から参照してください。

## 2. 検証システム (`Makefile`) について

コードの検証は、エージェントや開発者が個別にコマンドを叩くのではなく、必ず `make ci`（またはその他の `Makefile` タスク）経由で実行します。

- これには通常のTS型チェックやフォーマッタに加えて、本プロジェクト専用の**独自ルールチェックスクリプト（`scripts/check-ts-rules.py`等）**が組み合わされています。
- たとえば `any` 型や `@ts-ignore` の使用は、この独自スクリプトによって CI 上で厳格に弾かれます。TSのコンフィグを一時的に緩めても許容されないため注意してください。
